source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '15.0'
use_frameworks!

target 'salei' do
    pod 'SnapKit', '~> 5.6'
    pod 'SDWebImage', '~> 5.0'
    # 如果需要 WebP 支持，额外添加
    pod 'SDWebImageWebPCoder', '~> 0.3'
    # 添加图表库
    pod 'Charts', '~> 4.1.0'

end
